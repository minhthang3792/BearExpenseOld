import { StyleSheet } from "react-native";
import { Layout, Text } from "@ui-kitten/components";

export default function AddPaymentModal() {
  return (
    <Layout>
      <Text>Hello from AddPaymentModal</Text>
    </Layout>
  );
}

const styles = StyleSheet.create({});
